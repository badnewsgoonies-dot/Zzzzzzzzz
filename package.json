{
  "name": "golden-sun-nextera",
  "private": true,
  "packageManager": "pnpm@9",
  "scripts": {
    "build": "pnpm -r build",
    "dev:gs": "pnpm -F golden-sun dev || pnpm --filter golden-sun dev",
    "dev:nx": "pnpm -F next-era-battle dev || pnpm --filter next-era-battle dev",
    "type-check": "pnpm -r type-check || true",
    "test": "pnpm -r test || true",
    "lint": "pnpm -r lint || true",
    "circular": "pnpm -r circular || true"
  }
}
